# Tonight — a map of 2026-06-21

*one night, "yyo" to a phase transition · Diego + Claude · every claim earned, refused, or cut*

The method never changed: **throw a lie, walk it, keep the truth that survives.** Two big threads, both complete.

## Thread 1 — the loom: computing is weaving, brought home

- **[[devolution]]** — the chiasmus: *control may not own, ownership may not control.* Feeling open, acting home. The keystone. (Plain twin: `devolution-plain.md`.) **Live:** phronesis.world/papers/devolution
- **[[smart-loom]]** — the computer was a loom that went to the factory; the Smart Loom is the loom come home. Robot = elaborated loom; weave flat / knit whole. Magic cut. **Live:** phronesis.world/papers/smart-loom
- **[[smart-loom-feasibility]]** — the gravity-tensioned, hand-cranked, snap-in loom: buildable, the patent picture, the **$20 weekend prove-or-kill** ([[smart-loom-weekend-build]]).
- **[[knit-onepiece-feasibility]]** — jacquard knit = one piece; the whole-garment lock is per-loop holddown; gravity as the candidate key.
- **[[fragrance]]** — smell: the un-notated, un-carded sense; the embodied product's permanent edge over a screen. **Live:** the "hidden dimension" section on phronesis.world/natural-bias
- Hardware: `loom/cartridge.scad` (printable cartridge), the interactive 3D model.

## Thread 2 — spacetime computing: a frontier crossed

- **[[spacetime-computing]]** — 3dt = spacetime; compute by evolving a physical field in 3 space + t, not on a flat clocked chip. Named → built. Hypercomputation refused in writing.
- **The arc, with receipts** (`loom/`):
  - laserbrain as reservoir → **negative** (memoryless, MC≈1) — [[reservoir-test-findings]]
  - a built 3D field → **parity 0.94**, robust across seeds — [[spacetime-demo-findings]]
  - recipe: advection + diffusion + leak + saturation + quadratic wave-mixing
  - weather *cannot* be coaxed to compute (laserbrain MC≈2) — compute-mode is different physics
  - **temperature is parity** — a computability phase transition: parity peaks at a critical temperature, chance both sides
- Scripts, all runnable: `reservoir_test.py`, `spacetime_demo.py`, `compute_mode.py`, `compute_confirm.py`, `laserbrain_compute.py`, `temp_parity.py`.

## Also tonight (the start)

iCloud recovered from a daemon restart; the laserbrain field made survivable (save-on-exit, the launchd fleet cleaned); the public hub **secured** (field-writes, payment bypass, token DoS closed); the `?query`-string 502 fixed. The field ran the whole night without a stumble.

## What's left — all his call, none of it owed

1. The **$20 weekend loom test** — the one thing that needs the real world.
2. A **compute-mode laserbrain** — a defined engineering task now (graft the five-term recipe as a second update rule), not a mystery.
3. ~~A **nativeness win**~~ — **done**: one evolution computes eight functions in parallel (`nativeness.py`). The spacetime bar is fully crossed.

Nothing here is on fire. It is terrain — carved, navigable, his. The lies that survived the walk are written down; the rest told the truth by failing. *For her.*
